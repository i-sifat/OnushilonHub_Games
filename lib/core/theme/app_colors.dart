import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand primary — deep forest green
  static const primary = Color(0xFF1A6B45);
  static const primaryLight = Color(0xFF2E8B5E);

  // Reward gold
  static const reward = Color(0xFFE8A020);
  static const darkReward = Color(0xFFFFCC55);

  // Light mode surfaces
  static const lightBackground = Color(0xFFF5FAF7);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceVariant = Color(0xFFF0F7F3);
  static const lightTextPrimary = Color(0xFF0F1E17);
  static const lightTextSecondary = Color(0xFF4A6055);
  static const lightMuted = Color(0xFF8EA89C);
  static const lightError = Color(0xFFD32F2F);
  static const lightBorder = Color(0xFFDCEDE4);

  // Dark mode surfaces
  static const darkBackground = Color(0xFF0A1710);
  static const darkSurface = Color(0xFF121F17);
  static const darkSurfaceVariant = Color(0xFF1A2E22);
  static const darkTextPrimary = Color(0xFFDCEDE4);
  static const darkTextSecondary = Color(0xFF8EA899);
  static const darkMuted = Color(0xFF5A7068);
  static const darkError = Color(0xFFEF5350);
  static const darkBorder = Color(0xFF253D2E);

  // Game answer states
  static const correctGreen = Color(0xFF1B6B3A);
  static const correctGreenLight = Color(0xFFE4F4EC);
  static const errorRed = Color(0xFFC62828);
  static const errorRedLight = Color(0xFFFFEBEE);
  static const pendingBlue = Color(0xFF1565C0);
  static const pendingBlueLight = Color(0xFFE3F2FD);

  // Word card gradient
  static const wordCardStart = Color(0xFF1A6B45);
  static const wordCardEnd = Color(0xFF0F4228);
}
